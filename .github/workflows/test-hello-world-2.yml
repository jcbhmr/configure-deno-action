name: test/hello-world-2
on:
  schedule:
    - cron: "24 */6 * * *"
jobs:
  test-hello-world-2:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    defaults:
      run:
        shell: bash
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./test/hello-world-2
