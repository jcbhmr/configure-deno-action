inputs:
  format:
    default: Hello %s!

outputs:
  output: {}

runs:
  using: node20
  main: .using-deno/main
  pre: .using-deno/pre
  post: .using-deno/post

  pre-if: always()
  post-if: always()
.runs:
  using: deno1
  main: main.ts
  pre: pre.ts
  post: post.ts
