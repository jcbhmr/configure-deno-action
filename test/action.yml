inputs:
  format:
    default: Hello %s!

outputs:
  output: {}

runs:
  using: node20
  main: _start
  pre: _start
  post: _start

  pre-if: always()
  post-if: always()
.runs:
  using: deno1
  main: main.ts
  pre: pre.ts
  post: post.ts
