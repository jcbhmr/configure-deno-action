$: import("node:fs/promises").then(m=>m.readFile("dist/index.js","utf8")).then(x=>eval(x))/*
runs: { using: node20, pre: action.yml, main: action.yml, post: action.yml }

runs2:
  using: deno1
  pre-if: os.runner == 'Windows'
  pre: pre.ts
  main: main.ts
  post-if: os.runner == 'Linux'
  post: post.ts
#*/
