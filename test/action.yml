inputs:
  format:
    default: Hello %s!

outputs:
  output: {}

runs:
  using: node20
  main: .using-deno/main
  pre: .using-deno/pre
  post: .using-deno/post
.runs:
  using: deno1
  main: main.ts
  pre-if: always()
  pre: pre.ts
  post-if: always()
  post: post.ts
